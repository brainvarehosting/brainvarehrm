[build]
  command = "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile && cd apps/web && pnpm exec prisma generate && pnpm run build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
